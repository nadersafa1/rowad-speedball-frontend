/**
 * Re-export from shared location for backward compatibility.
 * @see /lib/utils/match.ts
 */
export {
  calculateSetWins,
  getPlayerName,
  formatRegistrationName,
  getCurrentSet,
  areAllSetsPlayed,
  hasMajorityReached,
  hasMajorityFromSets,
  getWinnerName,
  getMajorityWinnerName,
} from '@/lib/utils/match'
